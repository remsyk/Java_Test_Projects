CS622Date Assigned:________________________Due Date:__________________________Hints:Calendar cal = new GregorianCalendar();DateFormat fmt = DateFormat.getDateTimeInstance(DateFormat.FULL, DateFormat.FULL, Locale.US);TimeZone tze = TimeZone.getTimeZone("EST");fmt.setTimeZone(tze);System.out.println("Local Time EST: " + fmt.format(cal.getTime()));Submission:Create a zip file of all your source files using the filename A5_yourLastname.  Then, upload the zip file in the Assignment Section of the website.